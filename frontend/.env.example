# Backend API (no trailing slash)
VITE_API_URL=http://localhost:8000

# Same IANA zone as backend SCHEDULER_TIMEZONE (must match for adherence calendar + synthetic slots)
VITE_SCHEDULER_TIMEZONE=Asia/Kolkata

# Supabase — same project as backend; use anon (public) key only in the browser
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
