# Mock API (local): run `uvicorn mock_server:app --reload --port 8001` from `mock_api/`
VITE_API_URL=http://localhost:8001

# Real backend (local or deployed) — switch only this when integrating
# VITE_API_URL=http://localhost:8000

# Supabase (patient auth in production; optional for mock-only dev)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
